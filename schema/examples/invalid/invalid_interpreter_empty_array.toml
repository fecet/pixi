#:schema ./../../schema.json

[project]
channels = ["conda-forge"]
description = "Invalid interpreter examples - empty array"
name = "invalid-interpreter-empty-array"
platforms = ["linux-64"]
version = "0.1.0"

[dependencies]
python = ">=3.8"

[tasks]
# This should be invalid - empty array interpreter
empty-array-interpreter = { cmd = "echo 'test'", interpreter = [] }
